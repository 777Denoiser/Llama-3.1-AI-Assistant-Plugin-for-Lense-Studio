import Config from './AIConfig';

// Access the API endpoints
const llamaApiEndpoint = Config.llamaApiEndpoint;
const infoApiEndpoint = Config.infoApiEndpoint;

// Access the cache size
const cacheSize = Config.cacheSize;